# Танки в Лабиринте - Многопользовательская игра

2D игра на Python с использованием pygame, где два игрока управляют танками в лабиринте по локальной сети.

## Особенности

- 🎮 Два танка разного цвета (красный и синий)
- 🗺️ Общая карта с лабиринтом для всех игроков
- 🔫 Система стрельбы и попаданий
- 💀 При попадании танк умирает и возрождается на своей точке спавна
- 🌐 Сетевая игра по локальной сети

## Установка

1. Установите зависимости:
```bash
pip install -r requirements.txt
```

## Запуск

### Вариант 1: Использование главного меню

```bash
python main.py
```

Выберите опцию:
- `1` - Запустить сервер
- `2` - Запустить клиент и подключиться к серверу
- `3` - Выход

### Вариант 2: Запуск отдельных компонентов

#### Запуск сервера:
```bash
python server.py
```

Сервер запустится на порту 5555 и будет ожидать подключения клиентов.

#### Запуск клиента:
```bash
python client.py
```

Или с указанием IP адреса сервера:
```bash
python client.py <IP_адрес_сервера>
```

Например, если сервер на другой машине:
```bash
python client.py 192.168.1.100
```

## Управление

- **W, A, S, D** или **Стрелки** - Движение танка
- **Мышь** - Поворот пушки (пушка всегда смотрит на курсор мыши)
- **Левая кнопка мыши** - Выстрел

## Как играть

1. Запустите сервер на одной машине (выберите опцию 1 в меню или `python server.py`)
2. На той же или другой машине в локальной сети запустите клиент (опция 2 или `python client.py`)
3. Каждый игрок получает танк разного цвета
4. Используйте клавиатуру и мышь для управления
5. Стреляйте во вражеский танк, чтобы уничтожить его
6. Уничтоженный танк возродится на своей точке спавна

## Структура проекта

- `game.py` - Игровая логика (танки, пули, лабиринт, столкновения)
- `server.py` - Сервер для синхронизации игры
- `client.py` - Клиентское приложение
- `main.py` - Главное меню для запуска
- `requirements.txt` - Зависимости проекта

## Технические детали

- **Порт**: 5555
- **Протокол**: TCP sockets с JSON сообщениями
- **Частота обновления**: ~60 FPS
- **Разрешение**: 800x600

## Требования

- Python 3.6+
- pygame 2.5.2

## Примечания

- Максимум 2 игрока на сервере
- Для игры по сети убедитесь, что:
  - Оба устройства в одной локальной сети
  - Порт 5555 открыт в файрволе на сервере
  - Вы используете правильный IP адрес сервера при подключении

